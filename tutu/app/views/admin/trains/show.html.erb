<p id="notice"><%= notice %></p>

<p>
  <strong>Номер:</strong>
  <%= @train.number %>
</p>
<p>
	<strong>Текущая станция:</strong>
	<% if @train.current_station.present? %>
		<%= @train.current_station.title %>
	<% else %>
		Станция не указана.
	<% end %>
</p>

<p>
	<strong>Текущий маршрут:</strong>
	<% if @train.route.present? %>
		<%= @train.route.title %>
	<% else %>
		Маршрут не назначен.
	<% end %>
</p>

<p>
  <strong>Вагоны</strong>
  <% if @train.wagons.empty? %>
    <p>
    В составе поезда нет вагонов.
    </p>
  <% else %> 
    <ul>
      <% @train.wagons.by_number(@train.wagons_order).each do |wagon| %>
        <li><%= link_to "Вагон №#{wagon.number}", admin_wagon_path(wagon) %></li>
      <% end %>
    </ul>
  <% end %>
</p>

<p>
  <%= link_to "Добавить вагон",new_admin_train_wagon_path(@train) %>
</p>


<%= link_to 'Редактировать', edit_admin_train_path(@train) %> |
<%= link_to 'Назад к списку поездов', admin_trains_path %>
