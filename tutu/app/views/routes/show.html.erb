<p id="notice"><%= notice %></p>

<p>
  <strong>Название:</strong>
  <%= @route.title %>
</p>

<% if @route.trains.empty? %>
  <p>Поездов на маршруте нет.</p>
<% else %>
  <p>
  	<strong>На маршруте следующие поезда:</strong>
  </p>
  <ul>
    <% @route.trains.each do |train| %>
      <li><%= train.number %></li>
    <% end %>
  </ul>
<% end %>

<p>
  <strong> Станции в маршруте:</strong>
</p>

<p>
  <% if @route.railway_stations.empty? %>
    Станций в маршруте нет.
  <% else %>
  <table>
    <thead>
      <tr>
      <td width="100px">Название</td>
      <td>Порядковый номер</td>
      <td>Время прибытия</td>
      <td>Время отправления</td>
      </tr>  
    </thead>
    <tbody>      
      <% RailwayStation.ordered(@route).each do |station| %>      
        <tr>
          <td><%= station.title %></td>
          <td>
            <%= form_tag update_number_railway_station_path(station), method: :patch do %>
              <%= number_field_tag :number, station.number_in(@route) %>
              <%= hidden_field_tag :route_id, @route.id %>
              <%= submit_tag "Изменить" %>
            <% end %>
          </td>
        
            <%= form_tag update_time_railway_station_path(station), method: :patch do %>
              <td><%= time_field_tag :arrival_time, station.time_in(@route, :arrival_time) %></td>
              <td><%= time_field_tag :departure_time, station.time_in(@route, :departure_time) %>
              <%= hidden_field_tag :route_id, @route.id %>
              <%= submit_tag "Изменить" %></td>
            <% end %>
         
        </tr>
      <% end %>
    </tbody>
  </table>
  <% end %>
</p>

<%= link_to 'Редактировать', edit_route_path(@route) %> |
<%= link_to 'Назад к списку маршрутов', routes_path %>
